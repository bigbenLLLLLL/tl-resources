// Placeholder repository: database access logic (Prisma) should live here.
export const findExampleById = async () => {
  // implement DB access via Prisma client, e.g.
  // return prisma.example.findUnique({ where: { id } });
  return null;
};
